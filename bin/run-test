#!/usr/bin/env bash
set -e

# Grab version from file
tag=${1:-latest}
image="ghcr.io/nasa/harmony-flow-test:${tag}"

docker run \
  --platform linux/amd64 \
  -e EARTHDATA_USERNAME \
  -e EARTHDATA_PASSWORD \
  --rm \
  "${image}"

